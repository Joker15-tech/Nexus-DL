# ==============================================================================
#  NexusDL — requirements-all.txt (Tous les extras)
# ==============================================================================
#  Installation complète : core + web + gui + pdf + cbr + convert +
#  flaresolverr + socks + notify + sync.
#
#  Généré depuis pyproject.toml via :
#      uv pip compile pyproject.toml --extra all -o requirements-all.txt
#
#  Pour tout installer d'un coup :
#      pip install -r requirements-all.txt
# ==============================================================================

# --- Core ---
-r requirements.txt

# --- Web ---
-r requirements-web.txt

# --- GUI ---
-r requirements-gui.txt

# --- PDF avancé ---
pypdf==5.1.0
reportlab==4.2.5
chardet==5.2.0
freetype-py==2.5.1

# --- CBR (RAR) ---
rarfile==4.5

# --- Conversion images ---
pillow-avif-plugin==1.6.0 ; python_version < "3.13"
pillow-heif==0.21.0

# --- Cloudflare (FlareSolverr) ---
aiodns==3.2.0
brotli==1.2.0
pycares==4.5.0

# --- SOCKS5 ---
httpx-socks[asyncio]==0.13.1
python-socks[asyncio]==2.5.3
async-timeout==5.0.1

# --- Notifications ---
apprise==1.13.1
requests==2.34.2
requests-oauthlib==2.0.0
oauthlib==3.3.1
PySocks==1.7.1
markdown==3.10.3
click==8.5.0
certifi==2024.12.14

# --- Synchronisation cloud ---
webdavclient3==3.14.7
boto3==1.43.99
botocore==1.43.99
s3transfer==0.19.2
jmespath==1.1.0
python-dateutil==2.9.0.post0
urllib3==2.8.0
xmltodict==0.14.2
