# ==============================================================================
#  NexusDL — requirements-web.txt (Interface Web)
# ==============================================================================
#  Dépendances pour l'interface web (backend FastAPI + WebSocket + JWT).
#
#  Généré depuis pyproject.toml via :
#      uv pip compile pyproject.toml --extra web -o requirements-web.txt
#
#  Le frontend Next.js nécessite Node.js et pnpm (voir package.json).
# ==============================================================================

# --- Core ---
-r requirements.txt

# --- Framework web ---
fastapi==0.141.1
starlette==0.41.3
uvicorn[standard]==0.53.0
uvloop==0.22.1 ; sys_platform != "win32"
httptools==0.8.0
watchfiles==1.3.0
websockets==14.1
python-multipart==0.0.32

# --- Authentification & sécurité ---
python-jose[cryptography]==3.5.0
ecdsa==0.19.2
rsa==4.9.1
pyasn1==0.6.4
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Rate limiting ---
slowapi==0.1.10
limits==3.13.0
deprecated==1.3.1
wrapt==1.17.0

# --- Templates (Jinja2 pour certaines pages) ---
jinja2==3.1.6
markupsafe==3.0.3

# --- Sessions serveur ---
itsdangerous==2.2.0

# --- CORS & middlewares ---
python-cors==1.0.1

# --- Email (notifications optionnelles) ---
email-validator==2.3.0
dnspython==2.8.0

# --- Dépendances indirectes FastAPI ---
pydantic==2.13.5
pydantic-core==2.49.0
annotated-types==0.8.0
typing-extensions==4.16.0
sniffio==1.3.1
anyio==4.15.1
idna==3.20
h11==0.16.0
click==8.5.0
